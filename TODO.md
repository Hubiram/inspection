# TODO: Add Remove Button for Valve Entries in CreateSectionForm

- [x] Add removeValve function to filter out valve at specified index
- [x] Change layout to flexbox with inputs taking equal space and button at the end
- [x] Add conditional remove button (× symbol) at the right end, visible only when valves.length > 1
- [x] Style the remove button to match form theme (red color, hover effects, larger size)
